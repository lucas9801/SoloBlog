{
  "name": "my-game-tech-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node scripts/lint.js",
    "build": "node scripts/build.js",
    "check:layout": "node scripts/check-layout.js",
    "new:post": "node scripts/new-post.js",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name my-game-dev-blog",
    "preview": "node scripts/preview.js"
  }
}
